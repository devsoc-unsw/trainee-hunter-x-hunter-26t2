export default function Problems() {
  // TODO: on mount, listQuestions() into state (useEffect + useState).
  // render a table: name, difficulty, a tick if solved. each row links
  // to /problems/{id}
  return (
    <div className="page">
      <h1>problems</h1>
      <p>problem list goes here</p>
    </div>
  )
}
